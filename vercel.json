{
  "buildCommand": "rm -rf node_modules && npm install && ./node_modules/.bin/vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "functions": {
    "api/*.js": {
      "runtime": "nodejs20.x"
    }
  }
}
